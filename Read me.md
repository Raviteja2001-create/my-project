This project demonstrates how to create AWS infrastructure using Terraform (Infrastructure as Code).

The entire network architecture is provisioned using Terraform instead of manually creating resources in the AWS Console.